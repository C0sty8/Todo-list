# Todo-list
To-Do List application written in C, specifically for Windows. It utilizes a file-based persistence system to manage, track, and dynamically update multiple task lists structured around specific calendar dates
